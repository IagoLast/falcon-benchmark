{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "scripts": {
    "build": "lerna run build",
    "postinstall": "lerna bootstrap --hoist",
    "test": "lerna run test"
  }
}
